#!/bin/sh
set -e

# cd deps/libffi/

# ./configure --enable-static --disable-shared --disable-builddir --with-pic --program-prefix=$(pwd)/build/deps/libffi

# cd -

PROJECT_ROOT="$(pwd)"
BUILD_DIR="build/$(uname -m)-$(uname)"
mkdir -p $BUILD_DIR

cd $BUILD_DIR

cmake ${PROJECT_ROOT}

cd -
